# Write a Python program to create a lambda function that adds 25 to a given number passed in as an argument.
# sample input: 10
# sample output: 35

# Program:-


add_num = lambda a : a + 25
add = int(input("Enter the Number:-   "))
print(add_num(add))
